{
	"name": "obsidian-timeline-planner",
	"version": "1.0.0",
	"description": "Gantt Timeline planning for Obsidian. It's Vibe coded, but managed by an actual programmer, just not from the Web-dev field.",
	"main": "main.js",
	"scripts": {
		"styles": "node scripts/merge-styles.mjs",
		"dev": "node scripts/merge-styles.mjs && node esbuild.config.mjs",
		"build": "node scripts/merge-styles.mjs && npx tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [],
	"author": "",
	"license": "APUOL",
	"devDependencies": {
		"@types/node": "^20.10.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.20.2",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.4.5"
	}
}
